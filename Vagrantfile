# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure(2) do |config|
  config.vm.box = "bruntonspall/security-testing"
  config.vm.box_url = "http://www.brunton-spall.co.uk/security-testing.box"
end
